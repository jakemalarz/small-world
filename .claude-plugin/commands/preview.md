Start the Vite dev server and open the game in the browser.

1. Run `npm run dev` in the background
2. Wait for the server to be ready (look for "Local:" URL in output)
3. Open the URL in the default browser with `open http://localhost:5173`
4. Report the URL to the user

If the dev server is already running, just open the browser.
